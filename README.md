# Connection-pooling-in-prisma
A serverless backend API built with Hono and Prisma, deployed on Cloudflare Workers. This project was created as a learning exercise to understand serverless APIs, CRUD operations, and efficient database connection management (connection pooling) with PostgreSQL.

POST / → Create a new user (name, email, password)

GET /users → Retrieve all users (id, name, email)
